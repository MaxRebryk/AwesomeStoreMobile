import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	listWrapper: {
		paddingBottom: 70,
	},
	loaderContainer: {
		paddingVertical: 20,
		alignItems: 'center',
	},
});
